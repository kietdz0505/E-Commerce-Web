import React from 'react';
import './Spinner.css';  // Import file CSS cho animation

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="loading-spinner"></div>
    </div>
  );
};

export default Spinner;
